/**
 * Snapshot of the homepage field explorer the team half-liked.
 * Homepage uses the same UI via `Sectors` in `./sectors`.
 * Kept so we can re-import without archaeology:
 *   import { SectorsExplorer } from "@/components/home/sectors-explorer";
 */
export { Sectors as SectorsExplorer } from "./sectors";
